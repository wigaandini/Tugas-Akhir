# --- Core scientific stack ---------------------------------------------------
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0

# --- Dataset I/O -------------------------------------------------------------
openpyxl>=3.1

# --- Visualization -----------------------------------------------------------
matplotlib>=3.7
seaborn>=0.12

# --- Notebook environment ----------------------------------------------------
jupyter>=1.0
ipykernel>=6.25

# --- Progress bars -----------------------------------------------------------
tqdm>=4.65

# --- Classical ML (SVM baseline) ---------------------------------------------
scikit-learn>=1.3

# --- Deep learning (CNN & SCNN) — PyTorch with MPS support ------------------
torch>=2.1
torchvision>=0.16
torchmetrics>=1.2

# --- Training utilities ------------------------------------------------------
tensorboard>=2.14

# --- Myo armband communication (phase 3) ------------------------------------
pyserial>=3.5          # serial communication
bleak>=0.21            # Bluetooth LE for Myo